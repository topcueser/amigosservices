spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/topcueser/microservices-app-config-server
          search-paths: client-project-config
          clone-on-start: true
      #profile: development

server:
  port: 8888